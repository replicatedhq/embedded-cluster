apiVersion: embeddedcluster.replicated.com/v1beta1
kind: Config
metadata:
  name: "testconfig-velero-plugins"
spec:
  version: "testversion"
  extensions:
    velero:
      plugins:
        - name: velero-plugin-example
          image: docker.io/library/velero-plugin-example:latest
          imagePullPolicy: Always
