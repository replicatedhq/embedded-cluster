name: Use source from the local workspace

inputs:
  repository:
    description: Unused; the repository has already been checked out
    required: true
  tag:
    description: Unused; the local Git checkout determines the source revision

pipeline:
  - runs: |
      echo "Using source already checked out by the workflow"
