backupsEnabled: false
credentials:
  existingSecret: cloud-credentials
deployNodeAgent: true
{{- if .ReplaceImages }}
image:
  repository: '{{ (index .Images "velero").Repo }}'
  tag: '{{ (index .Images "velero").Tag }}'
initContainers:
- image: '{{ (index .Images "velero-plugin-for-aws").Repo }}:{{ (index .Images "velero-plugin-for-aws").Tag }}'
  imagePullPolicy: IfNotPresent
  name: velero-plugin-for-aws
  volumeMounts:
  - mountPath: /target
    name: plugins
kubectl:
  image:
    repository: '{{ (index .Images "kubectl").Repo }}'
    tag: '{{ (index .Images "kubectl").Tag }}'
configMaps:
  fs-restore-action-config:
    labels:
      velero.io/plugin-config: ""
      velero.io/pod-volume-restore: RestoreItemAction
    data:
      image: '{{ (index .Images "velero-restore-helper").Repo }}:{{ (index .Images "velero-restore-helper").Tag }}'
{{- end }}
nodeAgent:
  podVolumePath: /var/lib/k0s/kubelet/pods
snapshotsEnabled: false
