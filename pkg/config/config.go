/*
Package config provides the configuration for the helmbin binary
*/
package config

// DataDirDefault is the default path to the data directory
const DataDirDefault = "/var/lib/replicated"

// WinDataDirDefault is the default path to the data directory on Windows
const WinDataDirDefault = "C:\\var\\lib\\replicated"
