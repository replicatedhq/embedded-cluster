override: |-
  config:
    metadata:
      name: foo
    spec:
      test:
      - one
      - two
      emptyObject: {}
expected: |-
  metadata:
    name: foo
  spec:
    test:
    - one
    - two
    emptyObject: {}
