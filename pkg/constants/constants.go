/*
Package constants contains constants used throughout the project.
*/
package constants

const (
	// RoleController is the role for a controller node
	RoleController = "controller"
	// RoleWorker is the role for a worker node
	RoleWorker = "worker"
	// RoleControllerWorker is the role for a combined controller and worker node
	RoleControllerWorker = "controller+worker"
)
