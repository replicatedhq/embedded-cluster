package airgap

import "github.com/urfave/cli/v2"

// MaterializeAirgapFiles TODO: implement
// parse files from the airgap package, and write them to disk
// docker images need to be combined into a single tarball
// images from the app airgap package need to be extracted from the registry dump
// the airgap metadata needs to be checked against the version in this binary
func MaterializeAirgapFiles(c *cli.Context) error {
	return nil
}
