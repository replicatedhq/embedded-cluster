name: Use source from the local workspace

inputs:
  repository:
    description: Unused; Dagger supplies the working source
    required: true
  tag:
    description: Unused; the working source determines the revision

pipeline:
  - runs: |
      echo "Using source supplied by Dagger"
