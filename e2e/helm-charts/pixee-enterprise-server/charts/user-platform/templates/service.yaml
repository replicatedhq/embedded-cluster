apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-platform.fullname" . }}
  labels:
    {{- include "user-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-platform.selectorLabels" . | nindent 4 }}
