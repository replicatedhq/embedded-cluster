[build]
  publish = "dist"
  command = "npm run build"
  base = "/web"

[functions]
  node_bundler = "esbuild"
  included_files = ["../api/docs/**"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
